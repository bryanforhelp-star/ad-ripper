# Core web framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Settings
pydantic-settings>=2.2.0

# ORM / DB
sqlmodel>=0.0.16
sqlalchemy>=2.0.0

# HTTP client (for Meta API + downloader)
httpx>=0.27.0

# HTML parsing (snapshot scraper)
beautifulsoup4>=4.12.0

# Local Whisper transcription
# NOTE: this installs PyTorch (CPU); for GPU support install torch manually first.
openai-whisper>=20231117

# ffmpeg-python is NOT required; we shell out to the ffmpeg binary directly.
